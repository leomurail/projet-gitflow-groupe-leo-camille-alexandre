# 🧩 Guide de contribution

Merci de vouloir contribuer à **projet-gitflow-groupe-leo-camille-alexandre** !  
Ce projet a été réalisé dans le cadre d’un travail collaboratif, basé sur la méthode **Git Flow**.

---

## 🚀 Processus de contribution

### 1. Créer une branche
Toutes les nouvelles fonctionnalités doivent partir de la branche `develop`.

```bash
git checkout develop
git pull
git checkout -b feature/nom-feature
