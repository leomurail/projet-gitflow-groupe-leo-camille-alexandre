name: 🔍 Check required files & changelog

on:
  pull_request:
  push:
    branches: [main, develop]

jobs:
  check-required-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify presence of essential files
        shell: bash
        run: |
          echo "🔎 Vérification des fichiers essentiels..."
          missing=0

          for f in README.md CHANGELOG.md LICENSE CONTRIBUTING.md; do
            if [[ ! -f "$f" ]]; then
              echo "::error file=$f,line=1,col=1::❌ Le fichier requis '$f' est manquant à la racine du dépôt."
              missing=1
            else
              echo "✅ $f présent"
            fi
          done

          if [[ $missing -eq 1 ]]; then
            echo "::error::🚫 Vérification échouée — un ou plusieurs fichiers obligatoires sont absents."
            exit 1
          else
            echo "🎉 Tous les fichiers requis sont présents."
          fi

  changelog-consistency:
    name: Validate CHANGELOG vs latest tag
    runs-on: ubuntu-latest
    needs: [check-required-files]   # lance cette vérif après la précédente
    steps:
      - name: Checkout with full history (for tags)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # nécessaire pour récupérer tous les tags

      - name: Ensure CHANGELOG.md exists
        shell: bash
        run: |
          if [[ ! -f CHANGELOG.md ]]; then
            echo "::error file=CHANGELOG.md::❌ CHANGELOG.md est introuvable. Ajoutez ce fichier avant de continuer."
            exit 1
          fi
          echo "✅ CHANGELOG.md trouvé."

      - name: Find latest semver tag (vMAJOR.MINOR.PATCH)
        id: latest
        shell: bash
        run: |
          # Récupère et trie les tags sémantiques 'vX.Y.Z' par ordre décroissant
          git fetch --tags --force --prune >/dev/null 2>&1 || true

          latest_tag="$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname | head -n1)"

          if [[ -z "$latest_tag" ]]; then
            echo "::error::❌ Aucun tag au format 'vMAJOR.MINOR.PATCH' trouvé (ex: v1.2.3)."
            echo "   ➜ Créez un tag sémantique :"
            echo "      git tag v1.0.0 && git push origin v1.0.0"
            exit 1
          fi

          echo "🔖 Dernier tag détecté : $latest_tag"
          echo "tag=$latest_tag" >> "$GITHUB_OUTPUT"

      - name: Check if latest tag is referenced in CHANGELOG.md
        shell: bash
        run: |
          tag="${{ steps.latest.outputs.tag }}"

          # Échappe les points pour la regex
          esc_tag="$(printf '%s\n' "$tag" | sed 's/\./\\./g')"

          echo "🔎 Recherche de la version '$tag' dans CHANGELOG.md ..."
          echo "   (On accepte : '## vX.Y.Z', 'vX.Y.Z' ou un lien de section contenant la version)"

          if grep -Ei -q \
            "^(##+[^0-9]*${esc_tag}\b)|(^- .*${esc_tag}\b)|([^0-9A-Za-z]${esc_tag}([^0-9.]|$))" \
            CHANGELOG.md; then
            echo "✅ Cohérence OK : la version '$tag' est référencée dans CHANGELOG.md."
            exit 0
          else
            echo "::error file=CHANGELOG.md::❌ Incohérence : la dernière version taguée '$tag' n'est pas référencée dans CHANGELOG.md."
            echo ""
            echo "💡 Attendu : une section explicite pour '$tag', par exemple :"
            echo "   ## $tag"
            echo "   - Ajout : ..."
            echo "   - Correctif : ..."
            echo ""
            echo "Cas couverts :"
            echo " - Tag présent mais absent du changelog → échec"
            echo " - Changelog présent mais sans section correspondant au dernier tag → échec"
            echo " - Changelog et tag alignés → réussite ✅"
            exit 1
          fi
